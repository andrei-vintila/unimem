{
  "name": "@unimem/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Unimem desktop shell (Electron)",
  "//productName": "Electron derives app.getName() - and therefore the userData directory - from this, falling back to `name`. Without it the app would store data under 'Application Support/@unimem/desktop'.",
  "productName": "Unimem",
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "concurrently -k -n web,shell -c cyan,magenta \"pnpm --filter @unimem/web dev\" \"pnpm run dev:shell\"",
    "dev:shell": "wait-on tcp:3000 && pnpm run build && electron .",
    "build": "pnpm run build:main && pnpm run build:preload",
    "build:main": "tsc -p tsconfig.json",
    "build:preload": "esbuild src/preload/index.ts --bundle --platform=node --target=node20 --format=cjs --external:electron --outfile=dist/preload/index.js",
    "build:renderer": "pnpm --filter @unimem/web generate && node scripts/copy-renderer.mjs",
    "package": "pnpm run build && pnpm run build:renderer && electron-builder",
    "package:dir": "pnpm run build && pnpm run build:renderer && electron-builder --dir",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "icon": "electron scripts/render-icon.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "concurrently": "^10.0.5",
    "electron": "^43.4.0",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.28.2",
    "typescript": "^6.0.3",
    "wait-on": "^9.1.0"
  }
}
