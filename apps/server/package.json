{
  "name": "@unimem/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nitro dev",
    "test": "tsc -p tsconfig.test.json && node scripts/prepare-tests.mjs && node --test test/",
    "build": "nitro build",
    "preview": "nitro preview",
    "deploy": "nitro deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@unimem/analytics": "workspace:*",
    "@unimem/types": "workspace:*",
    "h3": "2.0.1-rc.26",
    "nitro": "3.0.260610-beta"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "wrangler": "^4.123.0"
  }
}
